using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Headset_Script : MonoBehaviour {
    private GameObject C7_H;
    private GameObject SN_H;
    private GameObject AC_H;

    //public static Vector3 Headset_Position;
   // public static Matrix4x4 Headset_TF;
	// Use this for initialization
	void Start () {
        //Headset_TF = new Matrix4x4();
    }
	
	// Update is called once per frame
	void Update () {
        //Headset_Position = GameObject.Find("[CameraRig]").transform.position;
        //Headset_TF = GameObject.Find("[CameraRig]").transform.localToWorldMatrix;

        //C7_H = QualisysRealTime.Unity.Q_To_A.Headset_Position[0];
        //SN_H = QualisysRealTime.Unity.Q_To_A.Headset_Position[1];
        //AC_H = QualisysRealTime.Unity.Q_To_A.Headset_Position[2];
    }
}
